const job = "developer";

const usuario = {
    firstName: 'Andres',
    lastName: 'Ceballos',
    age: 33,
    job: job,
}

console.log (`Mi nombre es ${usuario.firstName} ${usuario.lastName}, tengo ${usuario.age} años y soy ${usuario.job}`);